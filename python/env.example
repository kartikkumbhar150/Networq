
# Face Identity Threshold (Cosine Similarity)
# > 0.60 = same person | < 0.60 = different person
COSINE_THRESHOLD=0.60

# Anti-spoofing model directory (relative to python/)
SPOOF_MODEL_DIR=./models/silent_face
